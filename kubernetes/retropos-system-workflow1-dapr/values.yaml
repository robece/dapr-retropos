# Default values for retropos-system-dapr.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

dapr:
  secretStore:
    name: dapr.secretstore
    vaultName: 
    clientId: 
  bindingDurableRegistration:
    name: dapr.binding.durable-registration
    connectionString: 
    queueName: durable-registration
  stateConsumerState:
    name: dapr.state.consumer-state
    url: 
    masterKey: 
    database: 