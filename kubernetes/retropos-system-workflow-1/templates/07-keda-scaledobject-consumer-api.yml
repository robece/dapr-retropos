apiVersion: keda.k8s.io/v1alpha1
kind: ScaledObject
metadata:
  name: {{ .Values.consumerAPI.keda.scaledObject.name }}
  labels:
    deploymentName: {{ .Values.consumerAPI.keda.scaledObject.deploymentName }}
spec:
  scaleTargetRef:
    deploymentName: {{ .Values.consumerAPI.keda.scaledObject.deploymentName }}
  pollingInterval: {{ .Values.consumerAPI.keda.scaledObject.pollingInterval }}
  coolDownPeriod: {{ .Values.consumerAPI.keda.scaledObject.coolDownPeriod }}
  minReplicaCount: {{ .Values.consumerAPI.keda.scaledObject.minReplicaCount }}
  maxReplicaCount: {{ .Values.consumerAPI.keda.scaledObject.maxReplicaCount }}
  triggers:
  - type: {{ .Values.consumerAPI.keda.scaledObject.triggers.type }}
    metadata:
      # Required: queueName OR topicName and subscriptionName
      queueName: {{ .Values.consumerAPI.keda.scaledObject.triggers.queueName }}
      # or
      #topicName: functions-sbtopic
      #subscriptionName: sbtopic-sub1
      # Required: connection OR authenticationRef that defines connection
      #connectionFromEnv: SERVICEBUS_CONNECTIONSTRING_ENV_NAME # reference to a connection string in deployment
      # or authenticationRef as defined below
      #
      # Optional
      messageCount: "{{ .Values.consumerAPI.keda.scaledObject.triggers.messageCount }}" # default 5
    authenticationRef:
        name: {{ .Values.consumerAPI.keda.scaledObject.triggers.authenticationRef }} # authenticationRef would need either podIdentity or define a connection parameter