apiVersion: keda.k8s.io/v1alpha1
kind: TriggerAuthentication
metadata:
  name: {{ .Values.consumerAPI.keda.triggerAuthentication.name }}
spec:
  env: # Optional.
    - parameter: connection # Required.
      name: ServiceBusConnectionString # Required.